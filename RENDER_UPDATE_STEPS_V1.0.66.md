# FamilyCare Main V1.0.66 - Render update

## Include
- mobile navigation polish for Main;
- scroll fix retained;
- Test ecrane remains removed from menu;
- better phone layout for sidebar/menu and topbar;
- updated PWA cache.

## Render update
1. Upload the full contents of this archive to GitHub repository `familycare-main`.
2. Commit changes.
3. In Render, run `Manual Deploy -> Clear build cache & deploy` on `familycare-main`.
4. Do not change Environment Variables.
5. Do not run SQL again.

## Optional cleanup in GitHub
Delete old testing files if they still exist from older uploads:
- `pages/device-test.html`
- `styles/device-test.css`
